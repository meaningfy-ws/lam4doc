%- import "common/selectors.tex" as selectors with context
%- import "common/builders.tex" as builders with context

\part{Celex Classes}
\label{sec:celex-classes}

\VAR{ builders.build_top_collections(query_substitution_parameters=conf.celex_classes, level=level) }