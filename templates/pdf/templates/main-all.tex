%- extends "layout.tex"

%- block content
    %- set level = 2
    %- set prefixes, error = from_file(conf.template_path+'/prefixes.json').fetch_tree()
    %- set namespaces = namespace_inventory(prefixes)

    %- include "document_properties/part.tex" with context
    %- include "lam_classes/part.tex" with context
    %- include "celex_classes/part.tex" with context

%- endblock